syntax = "proto3";

package common.username.v1;

import "buf/validate/validate.proto";

message Username {
  string value = 1 [(buf.validate.field).string = {
    min_len: 4
    max_len: 63
    pattern: "[a-z]+$"
  }];
}
